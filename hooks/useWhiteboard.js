"use client";

/**
 * useWhiteboard 커스텀 훅 (레거시 호환성)
 *
 * ⚠️ 이 파일은 하위 호환성을 위해 유지됩니다.
 * 새로운 구현은 contexts/WhiteboardContext.js를 사용합니다.
 *
 * @deprecated WhiteboardContext를 직접 import하여 사용하세요.
 * import { useWhiteboard } from "@/contexts";
 */

import { useWhiteboard } from "@/contexts";

export default useWhiteboard;
